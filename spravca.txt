    .startsWith()
проверяет, начинается ли строка с указанных символов. Возвращает:
true — если строка начинается с заданной подстроки;
false — если не начинается.
    .trim() 
— убирает пробелы в начале и конце;
    .toLowerCase()
 — приводит к нижнему регистру для унификации обработки команд.
    command.
команда пользователя
    .includes()
используется для проверки наличия элемента в массиве или подстроки в строке. 
    split()
в JavaScript используется для разбиения строки на 
массив подстрок по указанному разделителю.
    .replace() 
заменяет часть строки на другое значение. 
    __dirname 
— это секретная встроенная переменная Node.js. 
Она автоматически хранит точный путь к той папке, 
где прямо сейчас лежит сам запускаемый файл save.js.
    JSON.parse()
 — преобразует строку JSON в объект.
    JSON.stringify()
 — преобразует объект в строку JSON.
    fs.writeFile() c Sync будет синхронный
— метод в модуле fs Node.js для асинхронной записи данных в файл. 
    fs.readFile c Sync будет синхронный
— метод в Node.js для асинхронного чтения файлов. 
    structuredClone()
делает копию чего-то там 




















/